<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.choi.calender.mapper.CalenderMapper">
    <!-- TypeHandler 등록 -->
<!--    <typeHandlers>-->
<!--        <typeHandler handler="com.choi.calender.application.handler.ListToStringTypeHandler" />-->
<!--    </typeHandlers>-->

    <insert id="insertDiary" parameterType="diaryBean">
        INSERT INTO tb_diary
        (
            dairy_date,
            day_of_week,
            weather,
            emotion,
            content
        )
        VALUES
        (
            #{date},
            #{dayOfWeek},
            #{weather},
            #{emotion},
            #{content}
        )
    </insert>

    <insert id="insertTarget" parameterType="targetBean">
        INSERT INTO tb_target
        (
            title,
            <if test="year != null and year != ''">
                year,
            </if>
            <if test="month != null and month != ''">
                month,
            </if>
            <if test="day != null and day != ''">
                day,
            </if>
            <if test="time != null and time != ''">
                time,
            </if>
            <if test="repeatYn != null and repeatYn != ''">
                repeat_yn,
            </if>
            type
        )
        VALUES
        (
            #{title},
            <if test="year != null and year != ''">
                #{year},
            </if>
            <if test="month != null and month != ''">
                #{month},
            </if>
            <if test="day != null and day != ''">
                #{day},
            </if>
            <if test="time != null and time != ''">
                #{time},
            </if>
            <if test="repeatYn != null and repeatYn != ''">
                #{repeatYn},
            </if>
            #{type}
        )
    </insert>

    <select id="selectYearTarget" parameterType="searchTargetBean" resultType="targetBean">
        SELECT
            no,
            title,
            type,
            year,
            month,
            day,
            time,
            repeat_yn,
            success_yn,
            delete_yn,
            reg_date
        FROM tb_target
        WHERE 0=0
          AND type = #{type}
          AND year = #{year}
    </select>

    <select id="selectMonthTarget" parameterType="searchTargetBean" resultType="targetBean">
        SELECT
            no,
            title,
            type,
            year,
            month,
            day,
            time,
            repeat_yn,
            success_yn,
            delete_yn,
            reg_date
        FROM tb_target
        WHERE 0=0
          AND type = #{type}
          AND year = #{year}
          AND month = #{month}
    </select>
</mapper>
